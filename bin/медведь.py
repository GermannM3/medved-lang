#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Медведь - Язык программирования на кириллице
Главный файл запуска
"""

import sys
import os

# Добавляем путь к исходникам
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))

из лексер импортировать Лексер
из парсер импортировать Парсер
из интерпретатор импортировать Интерпретатор

функция запустить(путь_к_файлу):
    # Читаем файл
    с открыт(путь_к_файлу, 'r', encoding='utf-8') как f:
        код = f.читать()
    
    # Лексирование
    лексер = Лексер(код)
    токены = лексер.лексировать()
    
    # Парсинг
    парсер = Парсер(токены)
    аст = парсер.разобрать()
    
    # Выполнение
    интерпретатор = Интерпретатор()
    интерпретатор.выполнить(аст)

если __имя__ == "__main__":
    если длина(sys.argv) < 2:
        вывод("Использование: python bin/медведь.py <файл.мед>")
        sys.exit(1)
    
    файл = sys.argv[1]
    если не os.path.существует(файл):
        вывод(f"Ошибка: Файл '{файл}' не найден")
        sys.exit(1)
    
    запустить(файл)
